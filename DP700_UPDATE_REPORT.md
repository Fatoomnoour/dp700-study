# تقرير إعادة بناء بنك أسئلة DP-700

## النتيجة

أُعيد بناء Screenshot السؤال الكامل لكل أسئلة DP-700 من ملفات PDF الأصلية، باستخدام حدود نصية مستخرجة من PDF عبر `pdftotext -bbox-layout`، ثم تحويل الصفحات إلى صور وقص بداية كل سؤال حتى بداية السؤال التالي. لا تعتمد الصور الجديدة على Screenshots القديمة.

## الإحصاءات

| المؤشر | النتيجة |
|---|---:|
| الأسئلة المعاد بناؤها | 279 |
| DP-700N1 / N2 / N3 | 118 / 43 / 118 |
| الأسئلة متعددة الصفحات | 200 |
| Screenshots كاملة جديدة | 279 |
| أجزاء صفحات محفوظة للمراجعة | 390 |
| Exhibits المرتبطة والمتحقق منها | 369 |
| أسئلة تحتاج مراجعة فعلية | 23 |
| أخطاء Validator | 0 |
| روابط صور مفقودة | 0 |

تُخزّن الصور الجديدة في `assets/questions/<source>-qNNN/full.png`، وتُحفظ الأجزاء الأصلية في `part-01.png` وما بعدها. أضيفت مصفوفة `sourcePages` إلى كل سجل سؤال، كما أضيف `questionScreenshot` و`screenshotParts` إلى بنك البيانات المركزي.

## قواعد تحديد الحدود

يبحث سكربت إعادة البناء عن موضع `QUESTION: N` داخل كل صفحة، ويحفظ إحداثيات البداية. إذا بدأ السؤال التالي في الصفحة نفسها، تنتهي الصورة قبله مباشرة. وإذا بدأ في صفحة لاحقة، تُضم صفحات الاستمرار إلى السؤال السابق، وتُقص الصفحة الأخيرة قبل موضع السؤال التالي. بهذه الطريقة لا تُعتبر نهاية الصفحة نهاية السؤال تلقائيًا.

## الفصل بين Screenshot وExhibits

يعرض الموقع الآن Screenshot السؤال الكامل في قسم مستقل، ويعرض Exhibits المرتبطة في قسم منفصل. لا يُستبدل Screenshot السؤال الكامل بصورة Exhibit. تم الإبقاء على مسارات الـ Exhibits الحالية بعد التحقق من وجودها ومن تطابق رقم السؤال وملف PDF في اسم كل ملف.

## الفحص البصري

تمت مراجعة السؤالين 15 و16 بصريًا بعد إعادة البناء. السؤال 15 يبدأ وينتهي داخل حدوده ويحتوي على الاختيارات والإجابة والتفسير دون تسرب من سؤال مجاور. السؤال 16 يضم استمراره في الصفحة التالية، ويحتوي على الاختيارات والإجابة والتفسير، ولا يحتوي على بداية السؤال 17.

تمت مراجعة السؤال 1 من بداية N1، وتأكدت المراجعة من أن نص الحالة الممتد إلى الصفحة التالية أصبح ضمن Screenshot السؤال 1 وأن الصورة تتوقف قبل السؤال 2. كما تم فحص مسارات عينات من بداية ووسط ونهاية N1، ومن N2، ومن بداية ووسط ونهاية N3 عبر اختبار HTTP محلي، مع اجتياز جميع الصور الجديدة للفحص.

## أدوات التنفيذ والتحقق

- `scripts/rebuild-dp700-question-bank.mjs` — يقرأ بنك الأسئلة الحالي، يطبّق حدود PDF الجديدة، ويولّد الحقول المركزية للصور والصفحات.
- `scripts/rebuild-dp700-images.py` — يستخرج إحداثيات الأسئلة، يرسم الصفحات من PDF، يقص الأجزاء، ويجمعها رأسيًا.
- `scripts/validate-dp700-bank.mjs` — يتحقق من الترتيب، الصفحات، حدود إعادة البناء، وجود Screenshot والأجزاء والـ Exhibits، وتطابق أسماء الملفات.
- `REVIEW_REQUIRED.md` — يحتوي فقط على العناصر التي لا يمكن تصحيحها بثقة بسبب نقص فعلي في المصدر أو الاختيارات، وليس على العناصر التي احتاجت إعادة بناء.

تم تشغيل فحوص JavaScript، و`git diff --check`، وValidator، واختبار HTTP محلي بنجاح. ظهرت تحذيرات PDF المتعلقة بخط غير صالح (`Invalid Font Weight`) أثناء القراءة، لكنها لم تمنع استخراج الحدود أو الصور.

## ملاحظة المصدر

ملفات PDF الأصلية المستخدمة موجودة في `source-pdfs/DP-700N1.pdf` و`source-pdfs/DP-700N2.pdf` و`source-pdfs/DP-700N3.pdf`. لم يتم تعديل مشروع DP-900.

## References

[1]: https://github.com/Fatoomnoour/dp700-study "DP-700 study repository"
[2]: https://github.com/Fatoomnoour/dp900 "DP-900 reference repository"
