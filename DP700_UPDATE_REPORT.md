# تقرير تحديث مشروع DP-700

## النطاق والمصدر

تمت مراجعة مستودع `Fatoomnoour/dp700-study` ومقارنته بنمط تنظيم الاختبار في مستودع DP-900 المرجعي. ملفا DP-700 المصدران موجودان داخل المستودع في `source-pdfs/`، ولذلك استُخدما كمصدر الاستخراج المتاح في بيئة التنفيذ الحالية: `DP-700N1.pdf` و`DP-700N2.pdf` و`DP-700N3.pdf`. لم يتم تعديل مستودع DP-900 أو محتواه.

## ما تم تنفيذه

تمت إضافة `scripts/validate-dp700-bank.mjs` كأداة فحص قابلة لإعادة التشغيل. تتحقق الأداة من عدد الأسئلة، عدم تكرار المعرفات أو أرقام أسئلة المصدر، ترتيب الأسئلة حسب ملف PDF ثم الصفحة ثم رقم السؤال، تطابق `sourcePage` مع الصفحة التي يظهر فيها السؤال فعليًا في PDF، وجود الاختيارات والإجابة، وجود جميع ملفات الصور، وتطابق اسم الصورة مع ملف PDF ورقم السؤال الصحيح.

تم تصحيح السؤال الداخلي `3075` ليصبح **مراجعة يدوية فقط** بدل أن يظهر كسؤال قابل للتصحيح، لأن مجموعة الاختيارات المستخرجة منه غير مكتملة (`A: B.` و`C: D.` فقط) ولا تسمح بتقديم إجابة موثوقة. كما تم إنشاء `REVIEW_REQUIRED.md` لتوثيق هذا السؤال وجميع العناصر الأخرى التي كانت أصلًا غير قابلة للتصحيح الآلي أو تعتمد على صورة/اختيارات ناقصة.

تم تحديث واجهة بنك الأسئلة والـ drill لإظهار **رقم صفحة PDF** بجانب كل سؤال، مع الإبقاء على الصور المرتبطة في موضعها داخل بطاقة السؤال. لا توجد أسئلة كاملة مكتوبة يدويًا داخل مكونات الواجهة؛ البيانات المركزية في `data/uploaded-pdf-bank.js` و`data/uploaded-interactions.js` هي التي تُستخدم للعرض والتفاعل.

## النتائج

| المؤشر | النتيجة |
|---|---:|
| عدد الأسئلة المستخرجة | 279 |
| DP-700N1 | 118 |
| DP-700N2 | 43 |
| DP-700N3 | 118 |
| عدد الصور/المعارض المرتبطة | 369 |
| عناصر تحتاج مراجعة يدوية | 23 |
| أخطاء Validator | 0 |
| روابط صور مفقودة | 0 |
| أخطاء ترتيب أو صفحات | 0 |

## مراجعة أول عشرة أسئلة

تمت مقارنة أول عشرة أسئلة مع نص صفحات المصدر باستخدام استخراج PDF صفحةً صفحة، والتحقق من رقم السؤال والصفحة وترتيب الظهور. النتائج: الأسئلة 1–10 موجودة بالترتيب، وصفحاتها هي `4, 5, 9, 10, 10, 11, 11, 12, 12, 15` على التوالي. كما اجتازت صور هذه الأسئلة فحص الاسم والوجود والارتباط بملف المصدر ورقم السؤال.

## ملفات تمت إضافتها أو تعديلها

- `scripts/validate-dp700-bank.mjs` — Validator قابل لإعادة التشغيل.
- `scripts/fix-dp700-q3075.mjs` — ترحيل تصحيحي صغير للسؤال غير القابل للتصحيح الآلي.
- `data/uploaded-interactions.js` — تعليم السؤال 3075 كمراجعة يدوية.
- `assets/js/app.js` — إظهار صفحة PDF في قائمة الأسئلة وواجهة drill.
- `REVIEW_REQUIRED.md` — قائمة العناصر التي لا ينبغي تخمين إجاباتها.
- `DP700_UPDATE_REPORT.md` — هذا التقرير.

## التحقق النهائي

تم تشغيل `node scripts/validate-dp700-bank.mjs` بنجاح، مع تحذيرات PDF غير المؤثرة من الخطوط (`Invalid Font Weight`) أثناء قراءة بعض الملفات. كما تم تشغيل فحوص بناء ثابتة لملفات JavaScript و`git diff --check`. الموقع يستخدم مسارات نسبية للبيانات والصور، ومتوافق مع النشر كموقع static على GitHub Pages.
